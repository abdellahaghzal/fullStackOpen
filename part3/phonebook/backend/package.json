{
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:front": "rm -rf dist/ && cd ../frontend/ && npm i && npm run build && cp -r ./dist ../backend/dist/ && cd ../backend/"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.2.3",
    "morgan": "^1.10.1"
  }
}
